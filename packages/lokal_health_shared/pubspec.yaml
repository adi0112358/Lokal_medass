name: lokal_health_shared
description: Shared models and seeded demo flows for Lokal MedAssist apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
